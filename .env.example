# Copy to `.env` (same folder as docker-compose.yml) and change the secrets.

# Gates the Whale/admin login at /whale-god — keep it secret.
WHALE_HOST_CODE=change-me-to-something-secret

# Password for the bundled Postgres container (the app builds DATABASE_URL from it).
POSTGRES_PASSWORD=change-me-too
